ktor:
  application:
    modules:
      - com.example.ApplicationKt.module
  deployment:
    port: 8080
    host: 0.0.0.0

#database:
#  url: ${DB_URL:-jdbc:postgresql://localhost:5432/ktor_db}
#  driver: org.postgresql.Driver
#  user: ${DB_USER:-postgres}
#  password: ${DB_PASSWORD:-postgres}
#  poolSize: ${DB_POOL_SIZE:-10}